require:
  - podman
test: |
  podman run \
    -e PLAN="${PLAN}" \
    -e PLATFORM="${PLATFORM}" \
    -e SUBJECT="${SUBJECT}" \
    --privileged \
    -it \
    quay.io/mizdebsk/mbici-tf:latest /run.sh
  cat results-template.yaml >${TMT_TEST_DATA}/results.yaml
result: custom
